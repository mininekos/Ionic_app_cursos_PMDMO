import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Ionic_app_cursos_PMDMO',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
